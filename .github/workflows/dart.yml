# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Dart

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  flutter_test:
    name: Run flutter test and analyze
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v1
        with:
          java-version: "12.x"
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.3.0"
      # - run: flutter pub get
      # - run: flutter analyze
      # - run: flutter test

  # build_ios:
  #   name: Build flutter(IOS)
  #   needs: [flutter_test]
  #   runs-on: macos-latest
  #   steps:
  #     - uses: actions/checkout@v3
  #     - uses: subosito/flutter-action@v2
  #       with:
  #         channel: "stable"
  #         architecture: x64
  #     - run: flutter pub get
  #     - run: flutter clean
  #     - run:  flutter build ipa --export-options-plist=ExportOptions.plist cd ios && fastlane beta ipa:../build/ios/ipa/YourBundleName.ipa
  #     - name: Push IOS to Releases
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: release-ipa
  #         # Path to the release files
  #         path: build/ios/ipa/*.ipa
  # path: /Users/runner/work/kachuwa-app-dev/kachuwa-app-dev/build/ios/iphoneos/*

  build_appbundle:
    name: Build flutter(Android)
    # needs: [flutter_test]
    runs-on: ubuntu-latest
    steps:
    # - name: Checkout Repository
    #     uses: actions/checkout@v2  
  
      - name: Create Hello World Text File
        run: echo "Hello, World!" > build/app/outputs/text/hello_world.txt
  
      - name: Push APK to Releases
        # uses: ncipollo/release-action@v1
        with:
          repo: runzun1987/kachuwaAppPublicToUse
          artifacts: "build/app/outputs/text/hello_world.txt"
          tag: v1.0.${{ github.run_number }}
          token: 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
